---
title:  "Overthewire-Bandit Level 12 - xxd"
date:   2026-03-26 01:00:00 +0000
categories: [Linux]
tags: [Linux]
---

![image](/assets/img/overthewire.png)

---
Ref: 

- [Overthewire-Bandit12](https://overthewire.org/wargames/bandit/bandit13.html)

---

## Bandit Level 12
Level Goal
The password for the next level is stored in the file data.txt, which is a hexdump of a file that has been repeatedly compressed. For this level it may be useful to create a directory under /tmp in which you can work. Use mkdir with a hard to guess directory name. Or better, use the command “mktemp -d”. Then copy the datafile using cp, and rename it using mv (read the manpages!)

Commands you may need to solve this level
grep, sort, uniq, strings, base64, tr, tar, gzip, bzip2, xxd, mkdir, cp, mv, file

Helpful Reading Material
[Hex dump on Wikipedia](https://en.wikipedia.org/wiki/Hex_dump)


## SSH
```
ssh bandit12@bandit.labs.overthewire.org -p 2220
7x16WNeHIi5YkIhWsfFIqoognUTyj9Q4
```

1️⃣ Create a temporary file (most common)
Exampole:
```
Shelltmpfile=$(mktemp)
echo "Hello world" > "$tmpfile"
cat "$tmpfile"
```
Example output filename:
```
/tmp/tmp.xY8aP3kL
```

✅ File is:

unique
securely created
writable by the current user

#

2️⃣ Create a temporary directory
```
tmpdir=$(mktemp -d)
echo " Temp directory created at: $tmpdir"
```

Example:
```
/tmp/tmp.R4gQeM2s
```
#
3️⃣ Use a filename template
You control part of the name using XXXXXX
(⚠️ At least 3 Xs required, usually 6)
```
mktemp myfile.XXXXXX
```
#
4️⃣ Temporary file with extension
```
mktemp /tmp/data.XXXXXX.txt
```
#

5️⃣ Safe temporary file in a script (best practice)
```
tmpfile=$(mktemp) || exit 1

trap 'rm -f "$tmpfile"' EXIT

echo "Processing data..." > "$tmpfile"
```
#
6️⃣ Temporary directory inside a script
```
tmpdir=$(mktemp -d) || exit 1

trap 'rm -rf "$tmpdir"' EXIT

cp file1 file2 "$tmpdir/"
```
#
7️⃣ Specify temp location explicitly
```
mktemp --tmpdir=/var/tmp myprog.XXXXXX
```
or
```
mktemp -p /var/tmp myprog.XXXXXX
```
#
8️⃣ What NOT to do (important!)
❌ Unsafe
```
tmp=/tmp/myfile
echo "data" > "$tmp"
```
This can cause:
- race conditions
- security vulnerabilities

✅ Safe
```
tmp=$(mktemp)
```
#

✅ Quick one-line examples
```
mktemp
mktemp -d
mktemp /tmp/test.XXXXXX
mktemp /tmp/test.XXXXXX.log
```
✅ When you’ll see this in real life

- Shell scripts
- Installers
- System utilities
- Security‑safe scripting
- Bandit / CTF levels
#


Commands:
```
bandit12@bandit:~$ 
mktemp -d
```
```
/tmp/tmp.bl5weXXHI6
```
```
cp ./data.txt /tmp/tmp.bl5weXXHI6
```
Output:
```

```
